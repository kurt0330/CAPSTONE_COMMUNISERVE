// PATH: /src/components/ui/MainHeader.jsx
// Replaces the <header class="main-header"> block in every HTML file

export default function MainHeader() {
  return (
    <header className="main-header">
      <h1 className="web-title">COMMUNISERVE</h1>
    </header>
  );
}